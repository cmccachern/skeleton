version: 2
jobs:
  build:
    steps:
      - checkout
      - run:
        name: Build
        command: echo Building
      - run:
        name: pylint
        command: echo "Running pylint"
  test:
    steps:
      - checkout
      - run: echo "Testing"
